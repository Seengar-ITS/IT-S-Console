import React,{useEffect,useState}from'react';import{supabase}from'../lib/supabase.js';import{requireAuth}from'../lib/auth.js';import*as S from'../styles.js';
export default function ProjectDetail(){const id=window.location.pathname.split('/')[2];const[p,setP]=useState(null);const[k,setK]=useState([]);useEffect(()=>{requireAuth(window.location.href);supabase.from('console_projects').select('*').eq('id',id).single().then(({data})=>setP(data));supabase.from('api_keys').select('*').eq('project_id',id).then(({data})=>setK(data||[]));  },[id]);
if(!p)return React.createElement('div',{style:S.page},React.createElement('p',{style:S.muted},'Loading...'));
return React.createElement('div',{style:S.page},React.createElement('h1',{style:S.h1},p.name),React.createElement('p',{style:{...S.muted,marginBottom:'1.5rem'}},p.description||'—'),React.createElement('h2',{style:S.h2},'API Keys'),k.length===0&&React.createElement('p',{style:S.muted},'No keys.'),k.map(x=>React.createElement('div',{key:x.id,style:S.card},React.createElement('p',null,x.name),React.createElement('p',{style:{...S.muted,fontFamily:'monospace'}},x.key_prefix+'••••••••'))));}